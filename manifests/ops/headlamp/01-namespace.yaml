# Headlamp — dashboard do Kubernetes (in-cluster).
# Acesso: https://headlamp.lan — login via SSO no Authentik (OIDC).
# Fallback: login por token do SA headlamp-admin continua disponivel.
# RBAC: SA headlamp-admin -> ClusterRole headlamp-admin (admin com guarda-corpo;
# ver 03-cluster-role.yaml). Sem basic auth no ingress.

apiVersion: v1
kind: Namespace
metadata:
  name: ops
