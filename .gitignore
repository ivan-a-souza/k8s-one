# Kubeconfig
kubeconfig
*.conf

# Bind mounts de estado do cluster (etcd, containerd, kubelet, PKI, kubeconfigs, Ceph) — NUNCA commitar
data/

# Manifests Kubernetes — locais, aplicados via docker cp (não versionar)
manifests/

# Docker
.docker/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
