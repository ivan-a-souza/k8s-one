# Kubeconfig
kubeconfig
*.conf

# Bind mounts de estado do cluster (etcd, containerd, kubelet, PKI, kubeconfigs, Ceph) — NUNCA commitar
data/

# Manifests Kubernetes — locais, aplicados via mount (não versionar)
manifests/

# Segredos dos apps (envs do kustomize) — nunca versionar
manifests/apps/secrets/

# Docker
.docker/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
