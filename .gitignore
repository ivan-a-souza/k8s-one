# Kubeconfig
kubeconfig
*.conf

# Bind mounts de estado do cluster (etcd, containerd, kubelet, PKI, kubeconfigs, Ceph) — NUNCA commitar
data/

# Manifests Kubernetes — apps locais, aplicados via mount (não versionar)
manifests/apps/

# Segredos (envs do kustomize) — nunca versionar
manifests/apps/*/secrets/
manifests/built-in/*/secrets/
manifests/networking/*/secrets/
manifests/ops/*/secrets/
manifests/data/*/secrets/
manifests/platform/*/secrets/
manifests/argocd/*/secrets/

# Docker
.env
.docker/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Backups
*.bak.*
# Snapshots de etcd, PKI (CA keys) e kubeconfigs — NUNCA commitar
backups/
